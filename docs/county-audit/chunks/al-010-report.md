# Alabama County Audit Chunk 010

## Scope

This is the tenth Alabama county-audit chunk. It reviews the next `5` counties in the state checklist:

- Marengo
- Marion
- Marshall
- Mobile
- Monroe

## Findings

- `Marengo`, `Marshall`, `Mobile`, and `Monroe` resolve to `complete-county-source-found`.
- `Marion` currently resolves to `complete-state-source-only`.
- This chunk widened the Alabama evidence mix beyond the recent aquatic-heavy pattern. It added county-specific evidence from forestry, fisheries, county project documentation, and structured federal occurrence records.
- `Marengo` is the clearest example in this chunk that an official annual report can still be strong enough for county-specific evidence when it records a direct county confirmation.
- `Mobile` already had multiple mapped species in production, but this audit made the county-detail evidence more defensible by tying it to county-specific sources rather than relying only on inherited map coverage.

## Contradictions Preserved

- County-specific evidence in this chunk is still uneven in scope. Mobile has more than one local signal, while Monroe currently leans on a thin but structured aquatic record.
- Marion staying `statewide-only` does not mean the county lacks invasive species. It means the reviewed public source stack still did not surface county-specific invasive reporting strong enough for production use.
- Marshall's current county-specific evidence is real, but it is still article-based invasive-carp reporting rather than a full structured county dataset.

## Progress Snapshot

- Audit chunks completed in this update: `1`
- Alabama counties: `67` total, `50` completed, `74.63%`
- Alabama chunks: `14` total, `10` completed, `71.43%`
- Nationwide counties in current 50-state audit set: `3141` total, `50` completed, `1.59%`
- Nationwide chunks at `5` counties each: `629` total, `10` completed, `1.59%`

## Recommendation

1. Continue Alabama sequentially into chunk `011` so the audit trail stays easy to verify.
2. Keep reusing `USGS NAS` early for aquatic corridors, but do not let that crowd out forestry, county project, and regulatory sources that can strengthen non-aquatic counties.
3. When a county already has mapped species in production, keep using the audit to improve evidence quality and county-detail clarity instead of assuming existing map coverage already proves the right thing.
