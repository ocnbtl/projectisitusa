interface:
  display_name: "Isitusa Evidence Worker"
  short_description: "Execute bounded auditable evidence research"
  default_prompt: "Use $isitusa-evidence-worker to complete one bounded evidence job and return a validated manifest."
