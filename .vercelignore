# The browser-visible county projections remain versioned at this Git path.
# Vercel deployments omit the large build copy and proxy the same URLs to the
# immutable Git commit that triggered the build.
public/generated/research
